export const initialStateCliente = {}

export const UserReducerCliente = (state, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}